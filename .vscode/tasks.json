{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run",
      "type": "shell",
      "options": {"cwd": "${workspaceFolder}"},
      "command": "love ${workspaceFolder}",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}